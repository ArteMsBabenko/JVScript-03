function find(array, value) {
	console.log(array.indexOf(value));
}

let products = ["Water", "milk", "chocolate", "berrys", "bread" ,"cucumber"];

find(products, "Apples");
find(products, "Milk");